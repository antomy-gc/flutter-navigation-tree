name: example
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
#
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_navigation_tree:
    path: ..

dev_dependencies:
  flutter_lints: ^5.0.0

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true
